


class Node:
    def __init__(self, value):
        self.val = value
        self.next = None


def intersection_node(headA, headB):
    """ Will return the node at which the two lists intersect.
        --> pointing TO THE SAME REFERENCE IN MEMORY 
        If the two linked lists have no intersection at all, return None.
    """
''' create an empty variable to maintain the first seqential digit'''
    ## first = None
    ## while headA and headB:
'''check to see if current_a.value is equal to current_b.value:'''
    ## while current current_a != current_b:
    ##     current_a = current_a.next
    ##     current_b = current_b.next
    ##     return first
'''set the empty variable to first same node value'''
    ## if current_a == current_b:
    ##     first = current_a
    ##  while current_a.next == current_b.next:
    ##      current_a = current_a.next
    ##      current_b = current_b.next
    ## > if current_a != current_b:
    ## > return None
    ## else return first


    pass